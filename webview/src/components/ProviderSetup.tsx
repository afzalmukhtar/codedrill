/**
 * Provider Configuration Wizard
 *
 * First-run setup that guides users through configuring
 * at least one model provider. Also accessible via
 * "Configure Providers..." in the model selector.
 *
 * Steps:
 * 1. Choose provider type (OpenRouter / Ollama / Direct API / Custom)
 * 2. Enter API key or base URL
 * 3. Test connection
 * 4. Select default model
 * 5. Save to SecretStorage + config file
 */

export function ProviderSetup() {
  // TODO: Step wizard UI
  // TODO: Provider selection cards
  // TODO: API key input (masked)
  // TODO: Connection test button
  // TODO: Model list after successful connection
  return null;
}
