/**
 * Bottom Panel Layout
 *
 * Main panel layout with split view:
 * - Left pane (40%): Problem statement, timer, session info
 * - Right pane (60%): Chat messages, code blocks, context badges
 * - Bottom bar: Model selector, token count, chat input
 */

export function BottomPanel() {
  // TODO: Split pane layout (resizable)
  // TODO: Left: ProblemView + Timer
  // TODO: Right: Chat messages
  // TODO: Bottom: ModelSelector + ChatInput
  return null;
}
