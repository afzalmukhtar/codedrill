/**
 * File Context Provider
 *
 * Reads file contents from the workspace for @file mentions.
 * Respects token budget by truncating large files.
 */

// import * as vscode from "vscode";
// import { ContextAttachment } from "../../ai/providers/types";

export class FileContextProvider {
  // TODO: resolve(filePath: string, maxTokens?: number): Promise<ContextAttachment>
  // TODO: fuzzySearchFiles(query: string): Promise<string[]>
}
