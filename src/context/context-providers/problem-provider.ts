/**
 * Problem Context Provider
 *
 * Provides the current problem statement and test cases
 * for @problem mentions and as default context.
 */

// import { ContextAttachment } from "../../ai/providers/types";

export class ProblemContextProvider {
  // TODO: resolve(problemId: number): Promise<ContextAttachment>
  // TODO: resolveSolution(userCode: string): Promise<ContextAttachment>
}
