/**
 * LeetCode GraphQL Client
 *
 * Fetches problem details from LeetCode using the leetcode-query
 * npm package (GraphQL API).
 *
 * Features:
 * - Fetch problem by slug (title, description, examples, constraints)
 * - Fetch problem list by category/tag
 * - Rate limiting to respect LeetCode's API limits
 * - Caching fetched problems to SQLite
 *
 * Note: Basic operations work without authentication.
 * Authenticated operations (submissions) require a session cookie.
 */

// import { LeetCode } from "leetcode-query";

export class LeetCodeClient {
  // TODO: constructor()
  // TODO: fetchProblem(slug: string): Promise<LeetCodeProblem>
  // TODO: fetchProblemList(category?: string): Promise<LeetCodeProblemSummary[]>
  // TODO: fetchDailyChallenge(): Promise<LeetCodeProblem>
}
