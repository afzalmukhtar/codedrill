/**
 * Problem Statement Parser
 *
 * Parses and formats LeetCode problem data into our internal
 * Problem format. Handles HTML-to-markdown conversion, example
 * extraction, and test case parsing.
 */

export class ProblemParser {
  // TODO: parse(rawProblem: LeetCodeProblem): Problem
  // TODO: extractExamples(description: string): Example[]
  // TODO: extractConstraints(description: string): string
  // TODO: htmlToMarkdown(html: string): string
}
