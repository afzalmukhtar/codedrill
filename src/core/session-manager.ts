/**
 * Session Manager
 *
 * Orchestrates practice sessions by selecting one new problem and
 * one review problem from different categories (interleaved practice).
 *
 * Responsibilities:
 * - Start a new session: pick 1 new + 1 review problem
 * - Ensure interleaving: new and review come from different categories
 * - Track active session state
 * - Handle session completion and rating submission
 * - Coordinate with Scheduler and ProblemBank
 */

// import { Scheduler } from "./scheduler";
// import { ProblemBank } from "./problem-bank";

export class SessionManager {
  // TODO: startSession(): Promise<Session>
  // TODO: getCurrentSession(): Session | null
  // TODO: completeAttempt(problemId, rating, userCode, timeSpent): Promise<void>
  // TODO: endSession(): Promise<void>
}
