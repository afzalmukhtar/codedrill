/**
 * Problem Bank Manager
 *
 * Manages the collection of problems from curated lists and LeetCode.
 * Handles caching, selection, and retrieval of problem data.
 *
 * Responsibilities:
 * - Load problems from bundled JSON lists (NeetCode 150, Blind 75, etc.)
 * - Fetch problem details from LeetCode GraphQL API (on demand)
 * - Cache fetched problems in SQLite
 * - Select a new problem for a session (respecting category exclusions)
 * - Retrieve problem details by slug or ID
 */

// import { Repository } from "../db/repository";
// import { LeetCodeClient } from "../leetcode/client";

export class ProblemBank {
  // TODO: initialize(lists: string[]): Promise<void>
  // TODO: getNewProblem(excludeCategories?: string[]): Promise<Problem>
  // TODO: getProblemBySlug(slug: string): Promise<Problem | null>
  // TODO: getProblemById(id: number): Promise<Problem | null>
  // TODO: getCategories(): Promise<string[]>
  // TODO: importFromList(listName: string): Promise<number>
}
