/**
 * OpenRouter Provider
 *
 * Implements LLMProvider for the OpenRouter API, which gives access
 * to 300+ cloud models from 60+ providers through a single API key.
 *
 * Uses the official @openrouter/sdk TypeScript SDK.
 *
 * Features:
 * - Auto-discover available models from OpenRouter API
 * - Streaming chat completions
 * - Cost tracking per request
 * - Fallback routing between providers
 */

// import { LLMProvider, ModelInfo, ChatRequest, ChatChunk } from "./types";

export class OpenRouterProvider /* implements LLMProvider */ {
  // TODO: constructor(apiKey: string)
  // TODO: getAvailableModels(): Promise<ModelInfo[]>
  // TODO: chat(params: ChatRequest): AsyncIterable<ChatChunk>
  // TODO: isAvailable(): Promise<boolean>
}
