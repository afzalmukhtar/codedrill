/**
 * Logger Utility
 *
 * Provides structured logging via VSCode's OutputChannel.
 * Supports log levels: DEBUG, INFO, WARN, ERROR.
 *
 * Logs are visible in: Output panel -> "CodeDrill"
 */

// import * as vscode from "vscode";

export enum LogLevel {
  DEBUG = 0,
  INFO = 1,
  WARN = 2,
  ERROR = 3,
}

export class Logger {
  // TODO: constructor(outputChannel: vscode.OutputChannel)
  // TODO: debug(message: string, ...args: unknown[]): void
  // TODO: info(message: string, ...args: unknown[]): void
  // TODO: warn(message: string, ...args: unknown[]): void
  // TODO: error(message: string, error?: Error, ...args: unknown[]): void
}
